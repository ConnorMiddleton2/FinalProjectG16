import { PortalPropertyBrowse } from "@/components/portal/PortalPropertyBrowse";

/**
 * Tenant welcome entry — browse properties, start an application, or sign in.
 */
export default function PortalStartPage() {
  return <PortalPropertyBrowse />;
}
