/**
 * Entry layout for public portal chooser pages.
 * Intentionally no tenant shell — visitors have not chosen a path yet.
 */
export default function PortalEntryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
