import { PortalPlaceholder } from "@/components/portal/PortalPlaceholder";

export default function MoveInPage() {
  return <PortalPlaceholder title="Move-in onboarding" description="Complete pre-move-in tasks after accepting a Harborline lease offer." />;
}